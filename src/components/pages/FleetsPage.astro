---
// B2B-лендинг «Автопаркам»: тёмная эстетика, CTA — расчёт в WhatsApp.
// TODO(контент): сверить с ТЗ раздел 4 (кейсы, калькулятор экономии — приём Webfleet).
import type { Lang } from '@config';
import { useTranslations } from '@/i18n';
import BaseLayout from '@/components/BaseLayout.astro';
import Section from '@/components/Section.astro';
import TrustBar from '@/components/TrustBar.astro';
import WhatsAppCTA from '@/components/WhatsAppCTA.astro';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);

const meta = {
  es: {
    title: 'GPS para flotas de camiones — GPS24 Benidorm',
    description:
      'Equipamiento GPS y dashcams para flotas: presupuesto por WhatsApp, instalación y actualización de mapas incluidas.',
  },
  en: {
    title: 'GPS for truck fleets — GPS24 Benidorm',
    description: 'GPS and dashcam equipment for fleets: quote via WhatsApp, installation and map updates included.',
  },
  ru: {
    title: 'GPS для автопарков — GPS24 Бенидорм',
    description: 'GPS-оснащение и видеорегистраторы для автопарков: расчёт в WhatsApp, установка и обновление карт включены.',
  },
}[lang];
---

<BaseLayout {lang} route="flotas" title={meta.title} description={meta.description}>
  <Section class="hero">
    <h1>{t('fleet.title')}</h1>
    <p class="lead">{t('home.fleetLead')}</p>
    <div class="hero__cta">
      <WhatsAppCTA {lang} label={t('cta.quote')} />
    </div>
  </Section>
  <Section>
    <TrustBar {lang} />
  </Section>
</BaseLayout>
